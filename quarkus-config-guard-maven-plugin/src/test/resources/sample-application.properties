# Sample Quarkus application.properties for testing
# This file intentionally contains violations for lint testing.

# --- Runtime properties (OK to use expressions) ---
quarkus.http.port=${HTTP_PORT:8080}
quarkus.http.host=0.0.0.0
quarkus.log.level=${LOG_LEVEL:INFO}

# --- Build-time properties with violations ---

# QCL001/QCL003: ERROR - build-time property using environment variable expression
%dev.quarkus.otel.exporter.otlp.enabled=${OTEL_EXPORTER_OTLP_ENABLED:false}

# QCL001/QCL003: ERROR - build-time property using environment variable expression
%prod.quarkus.otel.exporter.otlp.enabled=${OTEL_EXPORTER_OTLP_ENABLED:true}

# QCL001: ERROR - build-time property using expression
quarkus.swagger-ui.enable=${SWAGGER_ENABLED:false}

# QCL001/QCL002: ERROR+WARNING - build-time property using property reference
quarkus.otel.enabled=${some.other.property}

# --- Build-time properties set correctly (no violations) ---
quarkus.otel.traces.enabled=true
quarkus.swagger-ui.always-include=false
quarkus.package.type=jar
